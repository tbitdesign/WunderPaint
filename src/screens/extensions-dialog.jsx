/**
 * Kept as the stable import path for the extensions manager; the
 * implementation lives in ./extensions/ (dialog, sidebar, card, detail).
 */
export { ExtensionsDialog } from './extensions/dialog';
