{
	"name": "wpie-format-pack",
	"version": "1.5.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"test": "node --test tests/*.test.js",
		"pack": "npm run build && mkdir -p dist && rm -f dist/format-pack.zip && zip -r dist/format-pack.zip manifest.json extension.js style.css",
		"qa": "node tools/qa-dialog.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.28.1"
	}
}
