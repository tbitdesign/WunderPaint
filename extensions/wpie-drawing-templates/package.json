{
	"name": "wpie-drawing-templates",
	"version": "2.2.0",
	"private": true,
	"type": "module",
	"description": "WPIE extension: photos as printable drawing templates",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"qa": "node tools/qa-dialog.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.24.0"
	}
}
