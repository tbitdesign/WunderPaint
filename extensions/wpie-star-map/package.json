{
	"name": "wpie-star-map",
	"version": "1.9.1",
	"private": true,
	"type": "module",
	"description": "WPIE extension: the night sky over any place at any date, as a poster",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"qa": "node tools/qa-dialog.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.28.1"
	}
}
