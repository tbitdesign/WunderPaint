{
	"name": "wpie-marbling-studio",
	"version": "1.2.0",
	"private": true,
	"type": "module",
	"description": "WPIE extension: paper marbling on a virtual water bath - drops, combs, vortices and waves with exact mathematics",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"test": "node --test tests/*.test.js",
		"qa": "node tools/qa-dialog.mjs",
		"pack": "npm run build && mkdir -p dist && rm -f dist/marbling-studio.zip && zip -r dist/marbling-studio.zip manifest.json extension.js style.css"
	},
	"devDependencies": {
		"esbuild": "^0.28.1"
	}
}
