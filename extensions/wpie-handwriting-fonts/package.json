{
	"name": "wpie-handwriting-fonts",
	"version": "1.3.1",
	"private": true,
	"type": "module",
	"description": "WPIE extension: draw or scan an alphabet, get a real installable font",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"test": "node --test tests/*.test.js",
		"pack": "npm run build && mkdir -p dist && rm -f dist/handwriting-fonts.zip && zip -r dist/handwriting-fonts.zip manifest.json extension.js style.css",
		"qa": "node tools/qa-dialog.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.28.1",
		"opentype.js": "^1.3.4"
	}
}
