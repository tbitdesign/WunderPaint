{
	"name": "wpie-day-ring",
	"version": "0.7.0",
	"private": true,
	"description": "Day Ring extension for WunderPaint.",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"test": "node --test tests/*.test.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.24.0"
	}
}
