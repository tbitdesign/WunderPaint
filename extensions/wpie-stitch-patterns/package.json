{
	"name": "wpie-stitch-patterns",
	"version": "2.4.1",
	"private": true,
	"type": "module",
	"description": "WPIE extension: photos as counted craft charts",
	"scripts": {
		"build": "esbuild src/main.js --bundle --format=iife --minify --outfile=extension.js",
		"qa": "node tools/qa-dialog.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.24.0"
	}
}
